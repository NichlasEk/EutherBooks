# Local Models

This directory is intentionally ignored by Git except for documentation.

Use `scripts/download_piper_assets.sh` to download the Piper TTS binary and the first Swedish/English voices for local use.

Do not commit downloaded model weights directly to normal Git history. Keep exact source URLs, licenses, and checksums in documentation or scripts instead.
